# Diagrams

`pipeline-overview.mermaid` is the full stage-and-dependency graph, including licence
colour coding and the dataset attachments.

Note: dataset nodes in that diagram attach at **train time** only. The run-time path
reads no provider data. See `../architecture.md` for the split.
